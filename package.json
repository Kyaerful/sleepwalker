{
  "name": "sleepwalker-booking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "netlify dev",
    "deploy:preview": "netlify deploy --build",
    "deploy": "netlify deploy --build --prod"
  },
  "dependencies": {
    "@netlify/database": "2.0.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "netlify-cli": "27.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}
